export type SelectorItemType = {
  label: string;
  value: number;
};

export type DB_INSERT_PROPS = {
  month: number;
  date: number;
  startWorkTime: number;
  endWorkTime: number;
  startBreakTime: number;
  endBreakTime: number;
  imageUri: string;
};
